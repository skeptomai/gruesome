// Test that all array syntax compiles correctly
fn main() {
    // Test 1: Array creation
    let numbers = [1, 2, 3];
    let strings = ["hello", "world"];
    let empty = [];
    
    // Test 2: Array method calls
    numbers.add(4);
    let removed = numbers.remove(0);
    let size = numbers.length();
    let is_empty = empty.empty();
    let has_value = numbers.contains(2);
    
    // Test 3: Array property access
    let len = numbers.length;
    
    // Test 4: Arrays in control flow
    for item in numbers {
        print(to_string(item));
    }
    
    if !empty.empty() {
        print("Empty array is not empty");
    }
    
    // Test 5: Complex expressions with arrays
    let result = numbers.length + strings.length;
    if numbers.contains(removed) {
        print("Found removed item");
    }
}