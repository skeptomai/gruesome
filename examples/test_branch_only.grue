// Test branches without any objects to isolate the branch-specific issue

fn test_branch() {
    let x = 1;
    if x == 1 {
        print("Branch test passed");
    } else {
        print("Branch test failed");
    }
}

init {
    test_branch();
}