world {
    room test_room "Test" {
        desc: "test"
    }
}

fn test() {
    let items = [1, 2, 3];
    for item in items {
        print(item);
    }
}

init {
    player.location = test_room;
    test();
}
