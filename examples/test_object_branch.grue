// Test the combination of objects + branches that causes crashes
world {
    room test_room "Test Room" {
        desc: "A test room"
    }
}

fn simple_branch() {
    let x = 1;
    if x == 1 {
        print("Branch works");
    }
}

init {
    player.location = test_room;  // Object operation
    simple_branch();              // Branch operation
}