// Simple test case to isolate branch instruction issues
world {
    room test_room "Test Room" {
        desc: "A simple test room"
    }
}

fn test_branch() {
    let x = 1;
    if x == 1 {
        print("Branch test passed");
    } else {
        print("Branch test failed");
    }
}

init {
    player.location = test_room;
    test_branch();
}