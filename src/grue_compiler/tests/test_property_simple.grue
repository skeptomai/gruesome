// Test minimal property access - the most basic form that mini_zork uses

fn test_simple_property() {
    print("Testing simple property access...");
    // Try the absolute simplest property access: player.location
    let loc = get_location(player);  // Built-in function
    print("Got location successfully");
}

init {
    print("=== Simple Property Test ===");
    test_simple_property();
    print("=== Property Test Complete ===");
}