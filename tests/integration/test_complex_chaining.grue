fn main() {
    // Test complex property access chains
    let location_name = player.location.name;
    let container_status = player.location.container;
    
    // Test chained array operations  
    let numbers = [1, 2, 3];
    numbers.add(4);
    let length_after = numbers.length;
    
    // Test complex expressions with chaining
    if player.location.container {
        print("Player is in a container");
    }
    
    // Test property access in expressions
    let total = numbers.length + [4, 5, 6].length;
    
    print("Total: " + to_string(total));
}