// Test GET_PROP property access operations
world {
    room test_room "Test Room" {
        desc: "A simple test room."
    }
}

init {
    print("Testing GET_PROP property access...");
    
    // Test property assignment (PUT_PROP - already working)
    player.location = test_room;
    print("Set player location to test_room");
    
    // Test property access (GET_PROP - this is what we're testing)
    // Note: This should generate a GET_PROP instruction
    // For now, we'll test the compilation and instruction generation
    
    print("Property access tests completed");
}