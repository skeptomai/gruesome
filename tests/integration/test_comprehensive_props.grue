// Comprehensive test of property system
world {
    room start_room "Starting Room" {
        desc: "A room for testing properties."
    }
}

init {
    print("Testing comprehensive property operations...");
    
    // Test 1: PUT_PROP (property assignment)  
    player.location = start_room;
    print("✓ Property assignment (PUT_PROP) works");
    
    // Test 2: Property access in context (will generate GET_PROP when fully implemented)
    // Note: Direct property access syntax like `loc = player.location` 
    // would need expression evaluation improvements
    
    // Test 3: Verify the property system is intact
    print("Property system operational");
}